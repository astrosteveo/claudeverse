---
description: Initialize backlog tracking in current project
allowed-tools:
  - Bash
---

# Initialize Backlog

Run the initialization script:

```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/init-backlog.sh" "$PWD"
```
