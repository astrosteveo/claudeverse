---
description: Initialize project for iterative development workflow
argument-hint: [--with-state-tracking]
allowed-tools:
  - Bash
---

# Initialize Iterative Development

Run the initialization script and display output:

```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/init-project.sh" "$PWD" $ARGUMENTS
```
